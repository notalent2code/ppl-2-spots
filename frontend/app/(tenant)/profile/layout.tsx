"use client";

export default function ExploreLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <>
      <title>User Profile</title>

      <h1>User Profile</h1>

      {children}
    </>
  );
}
